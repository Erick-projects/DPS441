import React, {useState} from 'react';
import { View, Text} from 'react-native';
export default function Logout({ navigation }) {
    return (
    <View>
        <Text>Aqui el login</Text>
        </View>
);
}
